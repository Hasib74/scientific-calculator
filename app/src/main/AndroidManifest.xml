<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akhil.calculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="CalculatorActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".calculation.StandardCalculation" />
        <activity android:name=".calculation.ScientificCalculation" />
        <activity android:name=".converter.UnitWeight" />
        <activity android:name=".converter.UnitTemperature" />
        <activity android:name=".converter.UnitLength" />
        <activity android:name=".converter.UnitConverter" />
        <activity android:name=".converter.UnitArea" />
    </application>

</manifest>